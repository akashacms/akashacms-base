{
  "name": "akashacms-base-test",
  "version": "1.0.0",
  "description": "Test suite for @akashacms/plugins-base module",
  "main": "index.js",
  "scripts": {
    "test": "mocha ./index",
    "setup": "npm install && cd .. && npm install && npm install akashacms/akasharender#watcher --no-save",
    "clean": "rm -rf node_modules out"
  },
  "author": "David Herron <david@davidherron.com>",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0"
  }
}
